[loggers]
keys = root

[formatters]
keys = formatter

[logger_root]
level = DEBUG
handlers = stream_handler

[handler_stream_handler]
class = StreamHandler
level = DEBUG
formatter = formatter
args = (sys.stdout,)

[formatter_formatter]
format = [%(asctime)s] [%(levelname)s] [%(name)s]: %(message)s
